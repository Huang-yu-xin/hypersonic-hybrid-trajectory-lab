# M3-WCF1 W-Target s2 Design

- Valid W-by-s2 audit from corrected durable high-budget states only (CF2 inventory at CF0 common-grid points).
- Frozen W-target s2: [1.6, 1.25] (rule: max WIDEN configs, then WIDEN fraction, then lower s2, then grid index).
- Independence comes from new configs, new config-specific P_ref, new state identities, new seeds, and the prospective frozen selector.
