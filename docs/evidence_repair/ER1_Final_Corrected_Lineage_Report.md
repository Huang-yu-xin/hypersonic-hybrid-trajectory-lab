# ER-1 Final Corrected Lineage Report

## Current status

`FORENSICS COMPLETE — CORRECTED REPLAY PENDING`

The root cause, semantic contract, first contaminated stage and dependency
graph are frozen.  Event semantics schema version 2 is implemented and its
minimal deterministic tests pass.  No simulator calls have been made during
forensics.

The current valid scientific frontier is `RareTopo-M2-v0`, together with
structurally valid M3 mathematical/estimator machinery that predates the
contaminated empirical event mask.  M3-v0 is the first stage requiring a
protocol repair replay because its sample-level arrays were not persisted.

Stage-by-stage corrected headlines, gates and corrected tags will be appended
only while the corrected parent continues to authorize its child.  Until
then, M5-AR and M3-Q remain blocked, no R1–R6 route exists, and no corrected
descendant tag is authorized.
