# 2026-08-20 — ML-B1 First-Order Geometry Gate 实现

## 任务
RareTopo / hypersonic-hybrid-trajectory-lab 的 ML-B1（First-Order Geometry Gate）：
验证 virtual topology margin b、sign(b)↔exact topology、analytic gradient a=Phi*^T n*、
geometry direction v_geom 在真实 Sanger nonlinear hybrid simulator 上的正确性。

## 已完成
- HANDOFF AUDIT：branch feature/phase-h-uncertainty-risk，HEAD 7895712c（H2R），914 pytest 全绿，
  仅 pre-existing diffs（M pyproject.toml + untracked roadmap copy）。
- 新增 `src/hyptraj/uncertainty/topology_margin.py`：
  - `evaluate_topology_margin`（virtual continuation：prior 真实事件 + 固定 SANGER_ATM 不执行 critical exit，
    用 srti-candidate root（gamma: +→-）追踪极值；margin classification taxonomy）
  - `run_exact_topology`（真实 simulator 的 regime/skip/switch-signature/terminal oracle）
  - `analytic_margin_gradient`（prior 连续 STM + identity_reset_saltation 链乘，critical saltation 排除；
    a = Phi*^T n*，n* = sigma e_r，sigma=-1 for atm-exit channel）
  - `scaled_fd_margin_gradient` / `epsilon_sweep_fd`（scaled-space FD、pair gate、plateau 识别）
  - `geometry_direction` / `directional_boundary_scan` / `direction_controls`（v_geom、beta_local、随机/切线对照）
- 新增 `scripts/run_ml_b1_geometry_gate.py`（确定性生成器 → tests/data/ml_b1_first_order_geometry_v1.json）
- 新增 `tests/test_uncertainty/test_ml_b1_geometry_gate.py`（20 passed）
- 新增 `docs/phase_h/ml_b1_first_order_geometry.md`

## 关键发现（科学）
1. **margin 与 Phase-F certified clearance 吻合**：B1_N_side b=+0.1282 vs Phase-F phi=-0.128188；
   B1_N1_side b=-0.1695 vs +0.169139（b = -phi_F）。
2. **analytic vs scaled-FD**：修正 prior-saltation 匹配后各分量相对误差 < 1e-4，cosine≈1（B0/B1 N side）。
3. **同侧 FD 窗口**：extremal anchors |b|~0.1m，scaled FD 扰动 δb>|b| 即跨拓扑 → 列 invalid（pair gate 正确拒绝）。
   扩展 epsilon sweep 到 1e-8..3e-7 后全 4 列出现 clear plateau（1e-6 起 v 列失效）。
4. **solver 稳定性**：REF-0.1 vs REF-0.05 拓扑全部一致；production(max_step=20) 在全部 N1-side anchors
   漏掉最浅 skip（拓扑错）→ ML-B1 以 REF 为认证基准，production 仅 secondary audit。

## 环境备忘
- 项目 Python：C:\Users\huangyx\AppData\Local\hermes\hermes-agent\venv\Scripts\python.exe
  （hyptraj editable 安装于此；managed python 3.13.12 未装项目依赖）
- REF solver 配置：hyptraj.analysis.comparison_validation.REFERENCE_SOLVER_CONFIG (max_step=0.1)
  / REFERENCE_05_SOLVER_CONFIG (0.05)

## 待办
- [ ] 等待全量 snapshot 生成完成（5 anchors，~1.5-2.5h）
- [ ] 校验 snapshot + 跑 full pytest
- [ ] 输出 ML-B1 COMPLETE / READY FOR REVIEW 报告（git add/commit 待人工验收后由用户决定）

## 更新（18:48+）：ML-B1 全量验证完成
- 最终 snapshot 生成成功（5 anchors，1636s）：B0/B1/B2 × N/N1 side 全部 VALID_MARGIN
- Acceptance matrix 5/5 全 PASS：orientation / sign gate(80/80=100%) / FD plateau /
  gradient(cos=1.00000, L2~1e-6) / geometry identity / boundary(β_local ∈ bracket) / controls
- full pytest: 935 passed (914 + 21 ML-B1)
- 修复的三个问题：
  1. prior signature 自引用 bug（expected 用 full 而非 prior 截取）
  2. boundary scan λ 序列写死 1e-4 起点（改为相对 β_local 的 0.1-6 倍对数阶梯，支持负 β）
  3. bracket 检测对称化（regime 辅助判定，支持 N1 side 的 -→+ 变号）
- θ 列按对称性零列处理（大 ε 单点验证 ≈ 0，不参与 plateau）
- 关键科学结论：b = -phi_F（与 Phase-F clearance 一致）；analytic vs FD L2~1e-6；
  β_local 落在 exact boundary bracket 内；tangent 一阶导=0；production 漏 N1 浅 skip

## 更新（19:34+）：ML-B1 Git Commit 完成
- Commit: 78ecfb8eaa6dc38800af34f619cda4fa242a3585 "完成 ML-B1 一阶拓扑几何正确性验证"
  （parent=7895712 H2R，恰好 5 个 ML-B1 文件，7828 行）
- 事故与修复：.git/refs/heads/feature/ 目录缺失导致首次 commit 后 branch ref 未持久化，
  二次 commit 误产生孤儿 root commit c2a7cf0（280 文件）；已 mkdir + 写回 ref 恢复，
  孤儿 c2a7cf0 无引用（留待 gc）。pyproject.toml / roadmap / .workbuddy 保持未提交。
- 未 push / 未建 tag（等待后续指示）

## 更新（20:30+）：H3-0 Benchmark Definition Freeze 完成
- 任务：H3-0_Experiment_Task_RareTopo_Benchmark_Definition_Freeze.md —— H3 benchmark protocol freeze
  （definition-only：不实现 Geometry-IS / CEM / Flow / IS estimator，不生成 dataset）
- 新增 3 个交付物（git rule 仅允许 new docs/schema/tests，未触碰任何 frozen 文件）：
  1. `docs/phase_h/h3_benchmark_protocol.md` — 权威协议（event def / dataset schema /
     geometry metrics / evaluation protocol / claim boundary / final report 模板）
  2. `tests/data/h3_schema_v1.json` — machine-readable schema（schema_version=h3-sample-schema-v1，
     status=FROZEN；input / exact_simulator_output / geometry_information / nonlinearity_diagnostics
     + levels L0-L3 + metrics + ground-truth + experiment matrix + claim boundaries）
  3. `tests/test_h3_schema.py` — 38 passed（required fields / topology label consistency /
     geometry field availability / frozen ML-B1 cross-check / protocol intact）
- 关键冻结内容：事件 A={Z!=Z0}（仅 topology transition）；oracle=exact real hybrid simulator
  （禁止 sign(b) 替代）；epsilon_geo=||g(x)-g_hat(x)||；Delta_d=1-(d_geo·d_opt)/(||d_geo||||d_opt||)；
  topology label 7 字段 + mode-count 不变量 + switch 词汇表（sanger_atmosphere_exit/entry）

## 更新（20:40+）：H3-1-pre Dataset Generation Audit 进行中
- 任务：H3-1-pre_Dataset_Generation_Audit_Task.md —— 小规模 smoke dataset 验证 H3-0 frozen protocol
  在真实 hybrid simulator 上闭环（simulator → topology oracle → H3 schema → ML-B1 geometry → diagnostics）
- 生成器设计（临时脚本不入库，recipe 写入 artifact）：
  - 5 个 frozen ML-B1 anchors × 9 samples（center + 8 扰动）= 45 条
  - alpha_anchor = 2.0 × |β_local_ref|（线性模型下 ~31% transition 概率）；whitened_u = αz，z~N(0,I)，
    default_rng(2026+ai)；delta_x = S_A@whitened_u
  - oracle = run_exact_topology（REF-0.1, max_time=3000）；geometry = evaluate_topology_margin
    （nominal branch, check_prior_topology=False）+ analytic_margin_gradient + geometry_direction
  - diagnostics：linear_prediction = b_ref + a_ref^T·δx（frozen center geometry）；epsilon_geo=|b(x)-ĝ|；
    d = n^T f = sanger_atm_rhs 在 x* 处（extremum 处 ≈0）
- 实测耗时：B0 3.6s/sample（t*=366s），B2 22.5s/sample（t*=1250s，7 segments）；全程 ~15min
- 已知语义：B2 anchors 的 sample 若翻转到更低 N（segments < 5），critical segment 定位失败 →
  geometry UNAVAILABLE（如实记录，不掩盖）；d 在 extremum 处 ~1e-14（extremum 条件，非退化）
- 交付物：docs/phase_h/h3_1_pre_dataset_audit.md + tests/test_h3_dataset_audit.py（26 tests）+
  tests/data/h3_smoke_samples_v1.json（可选 artifact）
- 注意：tests/ 非 package，audit test 通过 sys.path 导入 test_h3_schema 的 validator（已验证可行）

## 更新（21:00+）：H3-1 方向切换 → Variance Leakage Driven Geometry-IS Validation
- 用户提供新 brief `H3-1_Variance_Leakage_Driven_Geometry_IS_Validation_Task.md`，明确"不按照旧文档执行"：
  H3-1-pre（dataset generation audit）废弃；H3-1 改为 **variance leakage benchmark**——
  研究 "When does hybrid topology geometry preserve Geometry-IS variance reduction?"
- 关键科学发现（来自前序原型）：Geometry-IS 失效主要由 **uncovered topology modes 的 variance leakage**
  主导，而非 local geometry error ε_geo。核心量 leak_k = ∫_{A_k} φ²/q dx（IS 方差 = (Σ leak_k - P²)/N）
- 已处理：停止 H3-1-pre 后台生成任务；删除未提交的 pre 文件（docs/phase_h/h3_1_pre_dataset_audit.md、
  tests/test_h3_dataset_audit.py）；生成器临时脚本 h3_smoke_gen.py 丢弃
- 新增：
  - `src/hyptraj/uncertainty/variance_leakage.py`（分析核心：design_point / importance_weights /
    mc_estimator / importance_estimator / ESS / VRF / decompose_modes / pearson/spearman）
  - `scripts/run_h3_variance_leakage.py`（生成器：L0 解析 half-space + L1 B0 / L2 B1_N1+B2_N /
    L3 B2_N1 真实 simulator configs；MC+IS 各 256 antithetic；margin batch 16 算 config ε_geo；
    figures 4 张 → results/phase_h3/）
  - `tests/test_h3_variance_leakage.py`（19 passed 单元 + L0 对照 + dataset regression 占位）
  - `docs/phase_h/h3_1_variance_leakage_analysis.md`（骨架，结果待填）
- 关键数学约定：标准化 z 空间 z~N(0,I)，δx=S_A(α_p z)；design point z* = -β_eff·α_dir，
  β_eff = β_local/α_p；leak_k 估计 = (1/N)Σ_{i∈A_k} w_i²；coverage = P_k^q/P_k
- L0 冒烟已验证：P_is≈0.0216（理论 Φ(-2)=0.0228）、VRF≈31.9、leak est 1.64e-3 vs 解析 1.73e-3
- 状态：完整生成器后台运行中（5 configs，预计 ~60-70min），完成后填文档数字、锁 regression、commit

## 更新（22:10+）：H3-1 完成（Variance Leakage Driven Geometry-IS Validation）
- 生成器完成（5003s）：6 configs（L0 解析 + L1 B0 + L2 synthetic multi-mode + L2 B1N1 + L2 B2N + L3 B2N1），
  MC/IS 各 256（synthetic 用 20000/200000），margin batch 16 算 config ε_geo
- **核心科学发现**：
  1. L2 synthetic multi-mode：P_S2=0.00575 ≪ P_S1=0.0653（11×）但 leak_S2=1.22 ≫ leak_S1=0.0128（95×），
     leak_fraction_S2=0.99 → **VRF=0.54 < 1（IS 比 MC 差）** —— "rare probability, dominant variance"
  2. **Silent leakage**：S2 解析 leak=1.505 vs IS 估计 1.221（200000 样本仅 5 个落入 A2）；
     ESS=21440 看似健康但掩盖 VRF=0.54（ESS 对 secondary-mode leakage 不敏感）
  3. 相关性（6 configs）：corr(leak,VRF) Pearson -0.459 / Spearman -1.0 vs corr(ε_geo,VRF) -0.267/-0.232
     → **variance degradation 主要由 leakage 解释，而非 ε_geo**
  4. 真实系统 v1 alpha（2-4|β|）下 transition 均为最近相邻单 mode（coverage 1.2-1.7，VRF 1.4-2.1）
- 交付物：variance_leakage.py（分析核心）+ run_h3_variance_leakage.py（生成器，支持 --resume）+
  h3_variance_leakage_dataset_v1.json（7 modes）+ test_h3_variance_leakage.py（28 passed）+
  h3_1_variance_leakage_analysis.md；figures 4 张 → results/phase_h3/（gitignored）
- 坑：--resume 反序列化丢失 extra 字段（analytic_leak）→ 已修复 _config_from_dict；L0 曾需手动 patch
- 测试：H3-1 28 + H3-0 38 + H0 41 + ML-B1 21 = **128 passed**；frozen 零改动
- 后续：H3-2 Adaptive Geometry-IS design（mixture proposal 覆盖 secondary modes）
- 测试环境：项目根 `.venv/Scripts/python.exe`（pytest 9.1.1）可用；managed 3.13.12 未装依赖

## 更新（22:40+）：H3-2 Leakage-Aware Adaptive Geometry-IS 进行中
- brief：PareTopo_H3-2_Leakage_Aware_Adaptive_Geometry_IS_Task.md（handoff 目录）
- 核心：H3-1 发现 leakage 主导 → H3-2 用 leakage 引导 adaptive mixture proposal
  q_mix = Σπ_k N(u_k*, I)，u_k* = -β_k·α（mode design point 从 discovery 加权中心估计）
- 新增：
  - `src/hyptraj/uncertainty/adaptive_geometry_is.py`：GaussianMixture（采样/log密度/权重）、
    mixture_importance_weights（log 空间）、discover_modes（复用 decompose_modes）、
    weight_strategies（probability/uniform/sqrt_leak/p05_l05）、per_mode_ess、leakage_reduction_ratio
  - `scripts/run_h3_adaptive_geometry_is.py`：实验 A（synthetic recovery）+ B（Sanger 宽 alpha 8|β|×2 configs）
    + C（3-mode stress）；支持 --synthetic-only
  - `tests/test_h3_adaptive_geometry_is.py`（19 passed 单元 + synthetic 回归 + dataset regression）
  - `docs/phase_h/h3_2_adaptive_geometry_is.md`（骨架）
- Synthetic 结果（已验证）：A 恢复 case VRF 0.935→69.8（leakage reduction 0.020，98% 削减）；
  C stress VRF 0.277→29.2（3 modes 全发现）；best strategy=probability（覆盖是主因、策略为二阶效应）
- Qian 判定：无 skip-count topology + 无 Sanger channel 几何 → B 实验聚焦 Sanger，Qian 留 H3-3+（文档记录）
- 状态：真实 Sanger 实验后台运行中（B1_N1_side + B2_N_side，alpha_factor=8，预计 ~40min）

## 更新（8/21 00:45）：H3-2 版本切换 → Variance-Optimal Leakage-Point 完成
- 用户提供新版 brief（PareTopo_H3-2_Variance_Optimal_Leakage_Point_Adaptive_Geometry_IS_Task.md），
  旧版（Leakage_Aware）废弃：停止旧后台任务、删除旧未提交文件（旧 adaptive 生成器/测试/文档/dataset）
- 核心科学更新：probability geometry（MPP, min‖x‖）≠ variance geometry（x_L = argmax_A ρ_L,
  ρ_L=φ²/q；Gaussian 下 x_L = argmin_A‖x+μ‖）；线性边界重合、弯曲边界（顶点偏移）分离
- 新增/修改：
  - `src/hyptraj/uncertainty/leakage_point_geometry.py`（新）：log_leakage_density / mpp /
    leakage_point / point_geometry（含解析对照测试锁住）
  - `src/hyptraj/uncertainty/adaptive_geometry_is.py`（增强）：leakage_power_weights(L^α) /
    hybrid_power_weights(P^γ L^{1-γ})
  - `scripts/run_h3_2_adaptive_geometry_is.py`（新，支持 --synthetic-only / --resume）：
    Exp A（线性多 mode 恢复）+ B（弯曲 stress, A2={u1>1.0+0.5(u2-1.5)²}, silent+leak 主导）+
    C（真实 Sanger B1N1/B2N, α=8|β|）；Stage B 三方法 ablation（M1 single/M2 topology mix/
    M3 leakage-point）+ 权重策略；figures 6-9
  - `tests/test_h3_2_adaptive_geometry_is.py`（17 passed）
  - `docs/phase_h/h3_2_leakage_point_adaptive_geometry_is.md`
- 关键科学结果：
  1. synthetic A：M1 VRF 0.149（失败）→ M2 26.1 / M3[prob] 26.2（恢复）；线性下 x*=x_L（dist 0）
  2. synthetic B：M1 0.123（深度失败）→ M2 14.6 / M3 12.1；S2 分离 0.775（x*=(1.23,0.83),
     x_L=(1.77,0.26)）→ **核心假设验证**
  3. 真实 C1（B1_N1_side）：真实系统 x*≠x_L 分离 1.136 ✓；C2 分离 0（事件概率大 MPP≈原点）
  4. 权重策略：π∝L^α 过度偏斜有害（VRF 0.17）；probability/p05_l05 平衡最优 ——
     **权重平衡 > 精确位置**（M2≈M3[prob]，新洞察修正 naive 预期）
  5. 真实系统 α=8|β| 事件概率 ~0.47（不 rare）→ VRF ~1.2；真实 multi-mode 未出现（H3-3 待办）
- 事故处理：完整运行两次被会话切换中断（后台任务句柄丢失）→ 加 --resume（复用已完成的真实实验）
  后重跑成功；log_leakage_density 符号 bug（‖z-μ‖² 应为 +）已修复并加解析测试
- 测试：H3-2 17 + H3-1 28 + H3-0 38 + H0 41 + ML-B1 21 = **145 passed**；frozen 零改动

## 更新（8/21 03:40）：H3-2 确定版重跑 + 模板报告
- 用户要求按模板填充 h3_2_result_analysis.md（公式 $$ 包围，9 章节）
- 发现生成器种子缺陷：Python 内置 hash() 有进程级随机化（PYTHONHASHSEED）→
  结果不可复现（C2 VRF 两次运行 1.238 vs 1.171）→ 改用 zlib.crc32 稳定种子
  （SEED + crc32(experiment_id) % 1000），与 H3-1 确定性约定一致
- 再次经历会话切换中断（后台句柄丢失）→ 给生成器加增量落盘 _persist（每实验完成立即写盘），
  --resume 复用已落盘实验 → 最终确定版完整落盘（03:32:57，4 实验）
- **关键科学修正**：确定版（crc32 种子）下真实 C1 的 x*-xL 分离 1.136 → 0.0 ——
  旧值为不稳定种子伪影，真实系统（单 mode、不 rare、近凸区域）未复现分离；
  synthetic B 分离 0.775 稳定。报告/权威文档已诚实改写（claim boundary 只保留 synthetic）
- 确定版数值：A M1 0.463→M2 26.0/M3 26.1；B M1 0.098→M2 14.4/M3 12.1；
  C1 M1 1.21 最优（M2 1.36/M3 1.26 无增益）；C2 M3[prob] 1.52 最优（M2 0.76 劣化）
- 文档更新：h3_2_result_analysis.md（模板报告）+ h3_2_leakage_point_adaptive_geometry_is.md
  （权威文档 §7/§8/§10/§11 全部对齐确定版）；fig6-9 用确定版重生成
- 测试：145 passed（H3-2 17 + H3-1 28 + H3-0 38 + H0 41 + ML-B1 21）

## H3-2 EXPERIMENT FREEZE (8/21 03:48-03:50)
- 状态：H3-2 FROZEN（确定版数据集 SHA-256 29e09cb6...）
- 冻结交付物：docs/phase_h/H3_2_experiment_freeze.md（286 行，含版本/commits/hash/
  final numbers/claims/limitations/reproducibility check/figure mapping/allowed operations）
- 在两份核心文档添加 FROZEN 状态标记（h3_2_result_analysis.md header banner，
  h3_2_leakage_point_adaptive_geometry_is.md 状态行 + dataset SHA-256 + Commit 补全）
- 冻结前验证：fig6/7 目检无旧 C1 分离伪影（C1/C2 退化单点、B S2 红线 0.775）；
  synthetic A/B fresh 重跑 vs dataset 1e-9 精度完全匹配（VRF + 距离）
- 冻结 commit 链：49d86a9 → 65dba42 → a03b364 → fa7c476 → 2171795
- 测试 145 passed 维持；dataset SHA-256 维持 29e09cb6...
- 后续规则（写明于 freeze 报告 §10）：仅允许文档引用/图注/排版修改；禁止
  改实验代码/随机种子/核心结果/figures 数据点。变更须发起新 stage（H3-3+）

## H3-2 C1 STABILITY AUDIT COMPLETE (8/21 10:12-10:50, Case A)
- 任务：裁决旧随机种子 C1 d_L=1.136 vs crc32 确定版 d_L=0 哪个真实
- 新增 scripts/run_h3_2_c1_stability_audit.py（只读复用 frozen 管线，workers 可配）
- **关键坑：workers=16 并行导致部分模拟异常被 _real_labels 吞成 NOMINAL(S0)，
  污染标签（假 S0 mode、VRF 漂移）→ 必须 workers<=4；workers=1/2/4 均逐位复现 frozen**
- 审计结果（Case A，separation 不稳定）：
  - multiseed（4 seeds, N=128）：d_L = {0.318, 0, 0, 0}（seed_old=1 唯一非零，VRF 1.23-1.25 稳定）
  - convergence（seed 2120, N/4N/16N）：d_L = {0, 0, 0.472} —— x_L 收敛但 x*(MPP) 随 N 翻动
  - landscape：max candidate 位置漂移（norm 0.37-0.69），density gap max/2nd 仅 1.01-1.21（平坦宽峰）
  - 旧 1.136 未被任何 seed/N 复现（最大观测 0.472）→ 采样伪影
- 机理解释：C1 mode 区域 ρ_L 近平坦（βκ≈0 无曲率），x*/xL 是同一有限点集上
  两个 argmin 的样本竞争，seed/N 变化即翻转；synthetic B (0.775) 有解析尖锐峰值不受影响
- 结论：H3-2 claim 不变——x*≠xL 仅在 synthetic curved 成立；real C1 未观察到稳定 separation
- 交付：docs/phase_h/H3_2_C1_stability_audit.md（audit 报告）+ audit 脚本 +
  3 个 H3-2 核心文档加交叉引用；commit 7d013f6 + 3bfc63f（refs 恢复）
- audit 数据（results/phase_h3/c1_audit_*.json）不入库（results/ 未跟踪）

## H3-2 FINAL FREEZE RECORD (8/21 10:56)
- 用户要求最终冻结记录：docs/phase_h/H3_2_experiment_freeze_final.md（196 行）
- 内容：commits 链（ebc2e1b → 3bfc63f → 7d013f6 → 49d86a9 → 65dba42 → a03b364 → fa7c476 → 2171795）、
  dataset SHA-256 29e09cb6...、final numbers（A/B/C1/C2 VRF 表 + B 分离 0.775 + 权重策略）、
  C1 audit 结论（Case A，1.136 = sampling artifact，multiseed + convergence + landscape 三表）、
  final claims（3 保留 + 3 not claimed）、limitations L1-L6、frozen deliverables、post-freeze 规则
- H3-2 = FROZEN；后续仅允许 Figure 制作/论文写作/caption 修改；禁止改结果/seed/扩范围
- commit ebc2e1b（refs 第 8 次恢复）
