"""M3-DS Corrected Directional-Sign Benchmark with Abstention Safety Axis.

This package is analysis-only: it reconstructs a dual-axis benchmark from
already frozen corrected evidence (M3-D2 confirmation, M3-D3 discovery,
M3-RF route decision). It never imports controller modules, never calls the
simulator, and never mutates corrected source artifacts.
"""
